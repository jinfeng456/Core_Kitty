using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace GK.WCS.Open.http.server {
   public class WMSServer:BaseServer {

       
        public object StockIn(List<String> param) {
            int type = int.Parse(param[0]);
            String boxCode = param[1];
            return null;//new WmsProcedure().wmsStockIn(type,boxCode);
          
        }
    }
    
}
